secrets.yaml
*.db
*.log
.storage/
test-e2e/screenshots/
node_modules/
.env
test-results/
playwright-report/

# Never back up .storage registries into the repo - they contain integration credentials
backups/core.*
backups/*config_entries*

# Python bytecode - swept in accidentally 2026-09-15 by a `git add -A`
__pycache__/
*.pyc

# Long-lived token for tools/go2rtc_eof_check.py's local websocket auth (live-config only)
tools/.ha_token
